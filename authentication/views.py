from django.shortcuts import render

# L'authentification OTP/MFA est implémentée dans authentication/api.py (Django Ninja).
# Ce fichier est conservé pour d'éventuelles vues Django classiques (templates).
